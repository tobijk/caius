#!/usr/bin/tclsh

package require markdown


#### MAIN ####
if {$::argc != 1} {
    puts stderr "Usage: markdown <input_file>"
    exit 1
}

set markdown [read [set fp [open [lindex $::argv 0]]]]
close $fp

puts [::markdown::convert $markdown]

